/* 
	* Defines: Icons used in this project
	* Rules: Icons needs to be defined along with their styles
	* Avoid: Using direct resource definition, import them from their respective files here 
*/

import 'package:flutter/material.dart';
// import 'colors.dart';

class Iconof {
  static Icon upArrow = const Icon(
    Icons.arrow_upward,
    // color: Colorof.primaryColor,
  );
}
