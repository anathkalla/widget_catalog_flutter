/* 
	* Defines: Colors used in this project
	* Rules: Colors can be defined using RGB(A) or hex only
	* Avoid: Using standard color names
*/

import 'package:flutter/material.dart';

class Colorof {
  Colorof._();

  static const Color primaryColor = Color(0XFF005E5D);
  static const Color secondaryColor = Color(0XFF00AF86);
  static const Color secondaryLightColor = Color(0XFFF9FBFA);
  static const Color accentColor = Color(0XFFFFC300);
  static const Color textDark = Color(0XFF424143);
  static const Color textLight = Color(0XFFF4F7F5);
}
